Entendido, corregiré el identificador del administrador a `'admin'`. Aquí está el plan actualizado:

### 1. Actualización de Constantes (`lib/constants.ts`)
- **Redefinir `SYSTEM_ROLES`**:
  - `ADMIN`: `'admin'` (Corregido según instrucción)
  - `SELLER`: `'vendedor'`
  - `USER`: `'user'`
- **Permisos**: Añadiré permisos básicos de `SALES` para el rol de vendedor y ajustaré `DEFAULT_ROLE_PERMISSIONS` para estos tres roles.

### 2. Esquema Prisma (`prisma/schema.prisma`)
- Mantendré la estructura dinámica (`model Role`) para garantizar la escalabilidad futura solicitada, permitiendo crear nuevos roles sin tocar el código o la BD posteriormente.

### 3. Seed de Base de Datos (`prisma/seed.ts`)
- Configurar el seed para crear únicamente estos 3 roles base (`admin`, `vendedor`, `user`) con sus respectivos permisos por defecto y marcarlos como roles de sistema.

Procederé con estos cambios.