Perfecto, ejecutaré la migración inicial para aplicar los cambios en la base de datos y poblarla con los nuevos roles.

### Plan de Ejecución

1.  **Ejecutar Migración de Prisma**:
    *   Comando: `npx prisma migrate dev --name init_roles`
    *   Esto aplicará el esquema a la base de datos y generará el cliente de Prisma actualizado.
    *   Automáticamente ejecutará el script `seed.ts` (configurado en `package.json`), poblando la base de datos con los roles `admin`, `vendedor` y `user`, y sus permisos correspondientes.

2.  **Verificación Post-Migración**:
    *   Confirmaré que el comando finalizó correctamente.
    *   (Opcional pero recomendado) Verificaré mediante un script rápido o consulta que los roles se crearon en la BD.

Procederé a ejecutar el comando de migración.