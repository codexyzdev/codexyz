La búsqueda ha revelado que debemos limpiar referencias obsoletas en los tipos de TypeScript y verificar que los componentes no estén usando permisos que ya no existen (como `CONTENT`).

### Plan de Limpieza y Actualización

1.  **Actualizar Tipos (`features/admin/types/permissions.ts`)**:
    *   Eliminar `ContentPermission` ya que el módulo `CONTENT` fue eliminado de las constantes.
    *   Añadir `SalesPermission` para reflejar el nuevo módulo de ventas.

2.  **Verificar Componentes Clave**:
    *   Los componentes de la interfaz (`roles-table`, `permission-matrix`, etc.) parecen ser dinámicos y deberían adaptarse automáticamente a los cambios en `lib/constants.ts` y la base de datos, pero haremos una revisión para asegurar que no haya hardcodes.
    *   `RoleGate` y `requirePermission` en las páginas (`app/admin/users/page.tsx`, etc.) usan permisos válidos (`users:view`, `roles:view`), por lo que **no requieren cambios** inmediatos, pero confirmaremos que no haya referencias a `content:*`.

3.  **Validación**:
    *   Asegurar que la aplicación compile correctamente tras eliminar el tipo `ContentPermission`.

No encontré referencias directas a `CONTENT` en el código de los componentes (la búsqueda mostró usos de `users:view`, `roles:view`), por lo que el cambio principal será en las definiciones de tipos.

Procederé a actualizar los tipos para que coincidan con la nueva estructura de permisos.